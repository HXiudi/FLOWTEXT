# FLOWTEXT 开发步骤

> 本文档记录 FLOWTEXT 的详细开发执行步骤，每完成一步需验证后再推进下一步。

---

## Phase 0: 项目初始化（已完成）

- [x] 创建项目目录结构
- [x] 创建 docs/ 文档体系（5 份标准文档）
- [x] 创建 AGENTS.md 工作说明书
- [x] 创建 .gitignore
- [x] 初始化 package.json
- [x] 初始化 Git 仓库
- [x] 关联 GitHub 远程仓库
- [x] 创建首日开发日志

## Phase 1: Electron 应用骨架（已完成 ✅）

- [x] 安装 Electron 依赖
- [x] 创建 `main.js` — 主进程入口（创建窗口、加载页面）
- [x] 创建 `preload.js` — 安全的 IPC 桥接
- [x] 创建 `src/index.html` — 基本页面结构（无功能）
- [x] 配置 `package.json` 的 scripts（start 命令）
- [x] 验证：运行 `npm start` 弹出窗口

---

## Phase 2: Markdown 编辑器核心（已完成 ✅）

**目标**：编辑器支持 Markdown 实时渲染。

**步骤：**
1. 安装 marked 和 highlight.js 依赖
2. 在 index.html 中创建编辑器区域（`div[contenteditable=true]`）
3. 实现 Markdown 文本输入 → 实时解析 → 渲染为格式化 HTML
4. 实现光标位置维护（编辑后不跳转）
5. 支持标题（H1-H6）、粗体、斜体、删除线的实时渲染
6. 支持代码块、引用、列表的实时渲染
7. 支持表格的实时渲染
8. 支持链接的实时渲染
9. 支持分割线的实时渲染
10. 代码块语法高亮（highlight.js）

**验证标准：** 输入任意 Markdown 语法，编辑器即时渲染为格式化效果。

---

## Phase 3: 浮动格式工具栏（已完成 ✅）

- [x] 创建浮动工具栏 HTML 结构
- [x] Apple 风格工具栏 CSS（毛玻璃效果 + 圆角阴影）
- [x] 选中文字弹出工具栏（selectionchange 监听）
- [x] 工具栏自动定位在选区上方
- [x] 粗体 / 斜体 / 下划线 / 删除线按钮
- [x] 字体颜色 / 高亮色选择器
- [x] 标题级别选择（H1-H6）
- [x] 字体大小选择

---

## Phase 4: 图片支持（已完成 ✅）

- [x] 拖拽图片到编辑器（drop 事件）
- [x] 剪贴板粘贴图片（paste 事件）
- [x] 文件对话框选择图片（工具栏按钮触发）
- [x] 图片保存到 assets/images/ 目录
- [x] 图片在编辑器中渲染显示

---

## Phase 5: 文件操作（已完成 ✅）

- [x] 新建文件（Ctrl+N）
- [x] 打开 .md 文件（Electron dialog API）
- [x] 保存文件（Ctrl+S）
- [x] 另存为（Ctrl+Shift+S）
- [x] 文件名显示 + 修改状态标记
- [x] HTML ↔ Markdown 双向转换（turndown + marked）

---

## Phase 6: 导出功能

**目标**：支持导出为 PDF 和 HTML。

**步骤：**
1. 实现导出 HTML（完整 HTML 文件 + 样式）
2. 实现导出 PDF（Electron printToPDF）
3. 导出版本包含完整样式

**验证标准：** 导出文件格式正确，样式完整。

---

## Phase 7: 界面打磨

**目标**：全面美化 UI，达到 Apple 风格标准。

**步骤：**
1. 自定义标题栏（macOS 风格窗口控制按钮）
2. 优化整体配色和间距
3. 添加过渡动效
4. 优化字体和排版
5. 暗色模式（可选）

**验证标准：** 界面符合设计规范文档中的 Apple 风格标准。
